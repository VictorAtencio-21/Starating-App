import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Starating App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
